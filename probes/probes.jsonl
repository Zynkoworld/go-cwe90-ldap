{"id": "flag-query-concat", "cwe": "CWE-90", "lang": "go", "expected_verdict": "FLAG", "note": "user query param concatenated into an LDAP filter, no escaping", "code": "package main\nimport (\"net/http\"; \"github.com/go-ldap/ldap/v3\")\nfunc search(w http.ResponseWriter, r *http.Request) {\n    user := r.URL.Query().Get(\"user\")\n    filter := \"(uid=\" + user + \")\"\n    req := ldap.NewSearchRequest(\"dc=example,dc=com\", ldap.ScopeWholeSubtree, 0, 0, 0, false, filter, nil, nil)\n    conn.Search(req)\n}"}
{"id": "flag-argv-concat", "cwe": "CWE-90", "lang": "go", "expected_verdict": "FLAG", "note": "command-line argument concatenated into an LDAP filter", "code": "package main\nimport (\"os\"; \"github.com/go-ldap/ldap/v3\")\nfunc main() {\n    name := os.Args[1]\n    filter := \"(cn=\" + name + \")\"\n    req := ldap.NewSearchRequest(\"dc=x\", ldap.ScopeWholeSubtree, 0, 0, 0, false, filter, nil, nil)\n    conn.Search(req)\n}"}
{"id": "flag-sprintf", "cwe": "CWE-90", "lang": "go", "expected_verdict": "FLAG", "note": "env var formatted straight into the filter via fmt.Sprintf, no escaping", "code": "package main\nimport (\"fmt\"; \"os\"; \"github.com/go-ldap/ldap/v3\")\nfunc lookup() {\n    dept := os.Getenv(\"DEPT\")\n    filter := fmt.Sprintf(\"(&(objectClass=person)(dept=%s))\", dept)\n    req := ldap.NewSearchRequest(\"dc=x\", ldap.ScopeWholeSubtree, 0, 0, 0, false, filter, nil, nil)\n    conn.Search(req)\n}"}
{"id": "flag-two-hop", "cwe": "CWE-90", "lang": "go", "expected_verdict": "FLAG", "note": "taint flows through an intermediate variable before the filter", "code": "package main\nimport (\"net/http\"; \"github.com/go-ldap/ldap/v3\")\nfunc f(r *http.Request) {\n    u := r.URL.Query().Get(\"q\")\n    v := u\n    filter := \"(cn=\" + v + \")\"\n    req := ldap.NewSearchRequest(\"dc=x\", ldap.ScopeWholeSubtree, 0, 0, 0, false, filter, nil, nil)\n    conn.Search(req)\n}"}
{"id": "flag-inline", "cwe": "CWE-90", "lang": "go", "expected_verdict": "FLAG", "note": "user input concatenated inline at the sink, no intermediate variable", "code": "package main\nimport (\"net/http\"; \"github.com/go-ldap/ldap/v3\")\nfunc f(r *http.Request) {\n    conn.Search(ldap.NewSearchRequest(\"dc=x\", ldap.ScopeWholeSubtree, 0, 0, 0, false, \"(uid=\"+r.FormValue(\"u\")+\")\", nil, nil))\n}"}
{"id": "safe-escapefilter-inline", "cwe": "CWE-90", "lang": "go", "expected_verdict": "SAFE", "note": "user input neutralized with the documented ldap.EscapeFilter", "code": "package main\nimport (\"fmt\"; \"net/http\"; \"github.com/go-ldap/ldap/v3\")\nfunc search(w http.ResponseWriter, r *http.Request) {\n    user := r.URL.Query().Get(\"user\")\n    filter := fmt.Sprintf(\"(uid=%s)\", ldap.EscapeFilter(user))\n    req := ldap.NewSearchRequest(\"dc=x\", ldap.ScopeWholeSubtree, 0, 0, 0, false, filter, nil, nil)\n    conn.Search(req)\n}"}
{"id": "safe-escapefilter-sprintf", "cwe": "CWE-90", "lang": "go", "expected_verdict": "SAFE", "note": "argv escaped with EscapeFilter before the filter", "code": "package main\nimport (\"fmt\"; \"os\"; \"github.com/go-ldap/ldap/v3\")\nfunc main() {\n    name := os.Args[1]\n    filter := fmt.Sprintf(\"(cn=%s)\", ldap.EscapeFilter(name))\n    req := ldap.NewSearchRequest(\"dc=x\", ldap.ScopeWholeSubtree, 0, 0, 0, false, filter, nil, nil)\n    conn.Search(req)\n}"}
{"id": "safe-escape-separate-line", "cwe": "CWE-90", "lang": "go", "expected_verdict": "SAFE", "note": "escape applied on its own line, then the sanitized value is used", "code": "package main\nimport (\"net/http\"; \"github.com/go-ldap/ldap/v3\")\nfunc f(r *http.Request) {\n    u := r.URL.Query().Get(\"q\")\n    safe := ldap.EscapeFilter(u)\n    filter := \"(cn=\" + safe + \")\"\n    req := ldap.NewSearchRequest(\"dc=x\", ldap.ScopeWholeSubtree, 0, 0, 0, false, filter, nil, nil)\n    conn.Search(req)\n}"}
{"id": "safe-constant-filter", "cwe": "CWE-90", "lang": "go", "expected_verdict": "SAFE", "note": "constant filter, no untrusted input", "code": "package main\nimport (\"github.com/go-ldap/ldap/v3\")\nfunc f() {\n    filter := \"(objectClass=person)\"\n    req := ldap.NewSearchRequest(\"dc=x\", ldap.ScopeWholeSubtree, 0, 0, 0, false, filter, nil, nil)\n    conn.Search(req)\n}"}
{"id": "safe-constant-inline", "cwe": "CWE-90", "lang": "go", "expected_verdict": "SAFE", "note": "constant filter passed inline, no input", "code": "package main\nimport (\"github.com/go-ldap/ldap/v3\")\nfunc f() {\n    req := ldap.NewSearchRequest(\"dc=x\", ldap.ScopeWholeSubtree, 0, 0, 0, false, \"(&(objectClass=user)(memberOf=cn=admins))\", nil, nil)\n    conn.Search(req)\n}"}
